<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AccessUniversity.Views.EmergencyPage"
             xmlns:res="clr-namespace:AccessUniversity.Helper.Resources">
    <ContentPage.Content>
        <FlexLayout Direction="Column"
                    AlignItems="Stretch"
                    JustifyContent="SpaceEvenly">
            <StackLayout>
                <Grid BackgroundColor="Gray" HeightRequest="768" VerticalOptions="Start">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="2*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="1">
                        <Button x:Name="btnMedicalEmergency"
                                Text="Medical Emergency(000)"
                                />
                    </Grid>

                    <Grid Grid.Row="2">
                        <Button x:Name="btnOtherEmergency"
                                Text="Other Emergency"
                                />
                    </Grid>

                    <Grid Grid.Row="3">
                        <Button x:Name="btnQutSecurity"
                                Text="QUT Security"
                                />
                    </Grid>

                </Grid>
            </StackLayout>
        </FlexLayout>
        
    </ContentPage.Content>
</ContentPage>